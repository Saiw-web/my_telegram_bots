version: 1
services:
  - type: worker
    name: my-telegram-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"  # Замените bot.py на имя файла с ботом
    autoDeploy: true